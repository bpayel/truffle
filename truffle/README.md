## Commands

# Install Node.js
Install the latest version of npm, run npm i -g npm

# Use NPM to install Truffle:
npm install -g truffle

truffle version

# truffle init 
# truffle compile
# truffle test
# truffle migrate
